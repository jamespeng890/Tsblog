{
  "name": "blog-platform",
  // ... 其他部分不变 ...
  "scripts": {
    "dev": "wrangler pages dev public",
    "build": "esbuild src/index.ts --bundle --outfile=public/_worker.js --format=esm --platform=node --external:cloudflare:workers",
    "deploy": "wrangler pages deploy public",
    "db:init": "wrangler d1 execute blog-db --file schema.sql"
  },
  // ... 依赖部分不变 ...
}
