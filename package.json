{
  "name": "blog-platform",
  "version": "1.0.0",
  "description": "A semi-static blog platform",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler pages dev public",
    "build": "esbuild src/index.ts --bundle --outfile=public/_worker.js --format=esm --platform=browser --external:cloudflare:workers",
    "deploy": "wrangler pages deploy public",
    "db:init": "wrangler d1 execute blog-db --file schema.sql"
  },
  "dependencies": {
    "marked": "^11.1.1",
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "wrangler": "^3.26.1",
    "@cloudflare/workers-types": "^4.20231218.0",
    "@types/node": "^20.10.5",
    "esbuild": "^0.19.11"
  }
}
